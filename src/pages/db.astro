---
import BaseHead from "../components/BaseHead.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import { SITE_TITLE, SITE_DESCRIPTION } from "../consts";
import { turso } from "../turso";
const { rows } = await turso.execute(
  "SELECT * FROM lunchmeals"
  // "SELECT * FROM lunchmeals WHERE date = '24.10.2024' ORDER BY dish;"
  // "SELECT * FROM lunchmeals WHERE date > '21.10.2024' AND date < '23.10.2024' ORDER BY date;"
  // "SELECT * FROM lunchmeals WHERE date = '2024-10-23' ORDER BY date;"
);
// mtn-kii-43_-_kii
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
  </head>
  <body>
    <Header />
    <main>
      <h1>🧑‍🚀 Hello, Astronaut!</h1>
      <div class="prose mx-auto">
        {rows.length}
        <ul>
          {
            rows.map((post) => (
              <li>
                <div>
                  {post.date}: {post.dish}
                </div>
              </li>
            ))
          }
        </ul>
      </div>
    </main>
    <Footer />
  </body>
</html>
